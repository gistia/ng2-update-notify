{
  "name": "ng2-update-notify",
  "version": "1.0.0",
  "description": "Posts to a slack channel when ng2 has a new release",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Felipe Coury",
  "license": "MIT",
  "dependencies": {
    "node-slack": "0.0.7",
    "redis": "^2.4.2",
    "crc": "^3.4.0"
  }
}
